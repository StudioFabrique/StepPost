<header class="mb-5">
    <nav class="bg-white shadow">
        <div class="sm:mx-auto lg:mx-0 px-2 sm:px-6 lg:px-8">
            <div class="relative flex items-center justify-between h-16">
                <div class="flex-1 flex items-center justify-center sm:items-stretch sm:justify-start">
                    <div class="flex-shrink-0 flex items-center">
                        <a href="{{ path('app_accueil') }}" aria-label="Home">
                            <img class="block h-10 w-auto" src="{{asset('build/images/logo.svg')}}"
                                alt="Step_post">
                        </a>
                    </div>

                </div>
                <div class="absolute inset-y-0 flex items-center sm:static sm:inset-auto sm:ml-6 ">
                    {% if app.user %}
                    <h1 class="text-black text-xl font-bold mr-3" id="dropdownDefault" data-dropdown-toggle="dropdown">
                        {{app.user.nom|title}}</h1>
                    <a data-turbo="false" href="{{ path('app_statistiques') }}" class="mr-3">Statistiques</a>
                    <a href="{{ path('app_logout') }}">Déconnexion</a>
                    {% endif %}
                </div>
            </div>
        </div>
    </nav>
</header>