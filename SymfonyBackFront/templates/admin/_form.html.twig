{{ form_start(form) }}
{{ form_widget(form) }}
<button class="text-blue-800 bg-blue-100 hover:bg-blue-700 hover:text-white p-2 rounded mt-4">{{ button_label|default("Créer l'administrateur") }}</button>
{{ form_end(form) }}